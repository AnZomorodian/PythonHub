## Packages
(none needed)

## Notes
Uses existing shadcn/ui components already in repo
API expected:
- GET /api/concepts → Concept[]
- GET /api/concepts/:id → Concept
No auth assumed; all requests use credentials: "include" per template
